# 数据库字段依赖清单

## 1. 表概览

访问两张表，通过 `CURRENT_RKZY_ID = RKZY_ID` 关联：

| 表名 | 用途 | 关系 |
|------|------|------|
| `WMS_HJ_KW` | 货架库位表 | 主表 |
| `WMS_RKZY` | 入库作业记录表 | LEFT JOIN / INNER JOIN |

---

## 2. WMS_HJ_KW（货架库位表）

### 字段明细

| 字段 | 类型 | 用途 |
|------|------|------|
| `HJ_KW_CODE` | VARCHAR | 库位编码。格式为 `排号(2位)+层号(2位)+列号(2位)`，例 `010203` = 第1排第2层第3列。|
| `KW_STATE` | INT | 库位状态码 |
| `CURRENT_RKZY_ID` | INT (FK) | 当前关联的入库作业 ID，外键指向 `WMS_RKZY.RKZY_ID` |

### KW_STATE 状态码枚举

| 码值 | 含义 | 分类 |
|------|------|------|
| `0` | 无效库位 | 异常 |
| `1` | 禁用 | 不可用 |
| `2` | 入库锁定 | 不可用 |
| `3` | 出库锁定 | 不可用 |
| `4` | 有货 | 已占用 |
| `5` | 无货（空库位） | 可用 |
| `6` | 空托盘 | 可用 |

---

## 3. WMS_RKZY（入库作业记录表）

### 字段明细

| 字段 | 类型 | 用途 |
|------|------|------|
| `RKZY_ID` | INT (PK) | 入库作业 ID |
| `MATERIEL_CODE` | VARCHAR | 物料编码 |
| `PALLET_NUMBER` | VARCHAR | 托盘号 |
| `TM` | VARCHAR | 托码 |
| `GG` | VARCHAR | 规格 | 
| `XH` | VARCHAR | 型号 |  
| `SJ` | VARCHAR | 供应商 |
| `RKSJ` | DATETIME | 入库时间 | 